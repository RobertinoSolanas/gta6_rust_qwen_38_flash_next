[package]
name = "gta-math"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Bounded context: linear algebra + procedural noise (pure, no deps)"

[dependencies]
