[package]
name = "gta-geo"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Bounded context: procedural mesh primitives, vertex encoding, spatial hash, polygon clipping"

[dependencies]
gta-math = { path = "../math" }
